# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# NeoForge 1.21.1 MDG needs a JDK 21 runtime for Gradle itself (not only the toolchain).
# Uncomment if your default java is newer (e.g. 25/26):
# org.gradle.java.home=/usr/lib/jvm/java-21-openjdk

# Parchment mappings
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Environment Properties
# https://projects.neoforged.net/neoforged/neoforge
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
neo_version=21.1.244
loader_version_range=[1,)

## Mod Properties
mod_id=flexibook
mod_name=FlexiBook
mod_license=GPL-3.0-only
mod_version=1.0.0
mod_group_id=io.github.PhantomDaze.flexibook
mod_authors=PhantomDaze
mod_description=Adaptive layout written books with an HTML-subset markup, i18n-aware reflow, and a custom screen.
