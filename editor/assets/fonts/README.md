# Deprecated

Default editor font is `flexibook:default` unihex ZIP under `assets/flexibook/font/`
(same file as the mod: `unifont_all-17.0.05.zip`).

Legacy Unifont OTF + ascii.png live in `editor/legacy/` and are **not** used by
App / PreviewCanvas / layout.
